node_modules
.next
out
build
coverage
package-lock.json
next-env.d.ts

# The master specification is the immutable source of truth for the project.
# Prettier must never rewrite it (it rewrites `*` bullets to `-`).
CLOVERCODE_MASTER.md

# Generated by `node scripts/seed-mrtaquito.mjs` from the PHOTOS table in that
# script. Prettier pads the columns of its credits table, which the next run
# would undo - a file that is rewritten on every seed cannot also be formatted.
docs/mrtaquito-photo-credits.md
